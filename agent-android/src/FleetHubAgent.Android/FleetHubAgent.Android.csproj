<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0-android</TargetFramework>
    <OutputType>Exe</OutputType>
    <RootNamespace>FleetHubAgent.Android</RootNamespace>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>

    <ApplicationId>net.arkeanos.fleethub.agent</ApplicationId>
    <ApplicationTitle>FleetHub Agent</ApplicationTitle>

    <!-- API 26 (Android 8.0). Notification channels arrive here and a foreground service
         cannot post its notification without one, so nothing below this could run the agent
         at all. Deliberately not raised to something newer: the devices most worth managing
         remotely are the old ones nobody wants to touch. -->
    <SupportedOSPlatformVersion>26</SupportedOSPlatformVersion>

    <!-- Android sorts upgrades by this integer and IGNORES the dotted version entirely. It
         must increase on every build a device might install over another, which is a
         different rule from every other version in this repo: it is a build counter, not a
         release number, and VersionGateTests deliberately does NOT tie it to
         AgentConfig.Version. -->
    <ApplicationVersion>1</ApplicationVersion>

    <!-- What a person reads, on the phone's own app info screen. This one IS the agent's
         version and must agree with AgentConfig.Version and the Core csproj; the three are
         held together by VersionGateTests, since there is no release script yet. -->
    <ApplicationDisplayVersion>0.2.2</ApplicationDisplayVersion>
    <Version>0.2.2</Version>
  </PropertyGroup>

  <!-- **Trim the whole app, not just its dependencies, in Release.** The default
       (`sdkonly`) leaves every managed assembly the app references intact, and this app now
       references BouncyCastle for one thing: Ed25519 signature verification over a
       hundred-byte manifest. Untrimmed that library costs more than five megabytes of an
       APK that is otherwise ten, and this APK is not just installed once: it is DOWNLOADED
       BY EVERY MANAGED PHONE ON EVERY RELEASE, over whatever connection and whatever data
       allowance that phone has. `full` is what makes the crypto dependency cost what it is
       worth rather than what it weighs.

       The risk `full` carries everywhere else is reflection: the linker cannot see a type
       resolved by name and removes it, and the failure lands at runtime rather than at build
       time. Two things make it acceptable here. The .NET Android SDK preserves the types the
       platform resolves reflectively (activities, services, receivers) through its own
       linker descriptors, and nothing in this app resolves a type by name itself.

       **System.Text.Json is the exception, and this comment used to say it was covered.** It
       was not. A fully trimmed app runs with reflection-based serialization DISABLED, so every
       JsonSerializer call without generated type info throws at runtime. 0.2.1 shipped that
       way: it could not store its enrollment token, send a report, read a command or read its
       own update manifest, while every workstation test passed. Every typed JSON call now goes
       through FleetHubAgent.Core's AgentJson context, and the test project runs with reflection
       off so a new reflective call fails there instead of on a phone.

       If a release ever crashes on startup with a MissingMethodException from a type nobody
       calls directly, this is the first thing to turn off. -->
  <PropertyGroup Condition="'$(Configuration)' == 'Release'">
    <AndroidLinkMode>full</AndroidLinkMode>
    <!-- The trimmer's own warnings for "this will not survive trimming" are errors here. The
         Core tests run with reflection off and catch Core; nothing tests this project, so the
         trimmer is the only thing that can see a reflective JSON call in the Android half.
         Before 0.2.3 these were warnings, and nobody reads warnings on a build that succeeds. -->
    <WarningsAsErrors>$(WarningsAsErrors);IL2026;IL3050</WarningsAsErrors>
  </PropertyGroup>

  <!-- Release signing, when this machine has been given the keystore.
       agent-android/signing.props is gitignored and holds a password, so it is treated like
       .env: real on the machine that cuts releases, absent everywhere else. Absent, this
       import does nothing and the build falls back to the SDK's debug keystore, which is what
       makes `dotnet build` work for anyone who only wants to compile.

       **A debug-signed build is not merely unsigned-for-release, it has a different
       identity.** The SSAID this agent reports as serial_number is derived from the signing
       key, so a device that runs a debug build and later a release build reports two different
       serial numbers and appears in the console as two machines. Copy signing.props.example
       and fill it in BEFORE the first install on any device you intend to keep. -->
  <Import Project="$(MSBuildThisFileDirectory)..\..\signing.props"
          Condition="Exists('$(MSBuildThisFileDirectory)..\..\signing.props')" />

  <ItemGroup>
    <ProjectReference Include="..\FleetHubAgent.Core\FleetHubAgent.Core.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- Only the logging plumbing. The protocol's own dependencies live in Core, which is
         what keeps this project's restore graph small enough that a build failure here is
         about Android and never about the agent. -->
    <PackageReference Include="Microsoft.Extensions.Logging" Version="10.0.6" />
  </ItemGroup>

</Project>

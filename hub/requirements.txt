requests
Authlib
python-dotenv
Flask
Flask-SocketIO
# Windows-only, and imported lazily by the two places that need it -- envfile.py's ACL
# tightening and the local temperature thread's COM init. The marker keeps `pip install -r`
# working off Windows, which is what lets tests/ run somewhere other than a hub machine.
# `wmi` used to sit beside this and was dropped: nothing in hub/ ever imported it, but the
# import at module scope made every module that imports app.py Windows-only.
pywin32; sys_platform == "win32"
waitress
# Backup encryption (AES-256-GCM). Authlib already depends on it, so this is a newly
# explicit requirement rather than a new download -- but backups.py must not be at the
# mercy of another package's dependency tree for the thing that encrypts them.
cryptography
# Active Directory sync (roadmap #4) ONLY. Pure Python, no compiler, ~1 MB -- taken
# because LDAP is a real ASN.1/BER protocol over TLS, not something to hand-roll the way
# the S3 SigV4 signer was. directory.py imports it lazily, inside the connect path, so a
# hub that never turns AD on neither needs it installed nor pays its import cost.
ldap3
